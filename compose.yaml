services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: /api
    container_name: react-analytics-dashboard-app
    ports:
      - "${DASHBOARD_PORT:-3000}:80"
    networks:
      - portfolio_suite_network

networks:
  portfolio_suite_network:
    external: true
    name: portfolio_suite_network
